#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../_nunjucks@3.2.4@nunjucks/bin/precompile" "$@"
else 
  exec node  "$basedir/../_nunjucks@3.2.4@nunjucks/bin/precompile" "$@"
fi
